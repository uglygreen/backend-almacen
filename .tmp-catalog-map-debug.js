const mysql = require('mysql2/promise');
function toNumber(value){ if (value===null || value===undefined || value==='') return 0; const numeric = Number(value); return Number.isFinite(numeric) ? numeric : 0; }
function toMoney(value){ return Number(toNumber(value).toFixed(2)); }
function cleanNullableString(value){ const cleanValue = (value ?? '').trim(); return cleanValue ? cleanValue : null; }
function buildCatalogImageUrl(imageName){ const cleanImageName = cleanNullableString(imageName); if (!cleanImageName) return null; const baseUrl='https://ferremayoristas.com.mx/assets/photos-img/'; const fileName = cleanImageName.replace(/\.[^.]+$/i, '.webp'); return `${baseUrl}${encodeURIComponent(fileName)}`; }
function mapCatalogRows(rows){ return rows.map((item) => ({ id: toNumber(item.id), codigo: cleanNullableString(item.codigo), clave: cleanNullableString(item.clave), descripcion: cleanNullableString(item.descripcion), marca: cleanNullableString(item.marca), imagen: buildCatalogImageUrl(item.imagen), imagenMarca: buildCatalogImageUrl(item.imagenMarca), claveProdServ: cleanNullableString(item.claveProdServ), claveUnidad: cleanNullableString(item.claveUnidad), unidad: cleanNullableString(item.unidad), almacen: item.almacen === null || item.almacen === undefined ? null : toNumber(item.almacen), precio: toMoney(item.precioRegular), iva: toNumber(item.iva), lote: toNumber(item.lote), descuento: toNumber(item.descuento), precioConDescuento: toMoney(item.precioDescuento) })); }
(async()=>{
const conn = await mysql.createConnection({ host: '192.168.1.250', port: 3306, user: 'web', password: 'webfmolvera17', database: 'datosb' });
const sql = `SELECT inv.ARTICULOID AS id, TRIM(inv.CLVPROV) AS codigo, TRIM(inv.CLAVE) AS clave, TRIM(inv.DESCRIPCIO) AS descripcion, TRIM(inv.XXMARCA) AS marca, TRIM(inv.XMCA_IMAG) AS imagenMarca, TRIM(inv.XIMAGEN2) AS imagen, CASE WHEN inv.CLAVEPRODSERV IS NULL THEN NULL ELSE LPAD(TRIM(CAST(inv.CLAVEPRODSERV AS CHAR)), 8, '0') END AS claveProdServ, 'H87' AS claveUnidad, TRIM(uni.UNIDAD) AS unidad, alm.ALMACEN AS almacen, IFNULL(inv.LOTE, 0) AS lote, IFNULL(inv.INVDESCUENTO, 0) AS descuento, IFNULL(pre.PIMPUESTO, 0) AS iva, IFNULL(ROUND((pre.PRECIO + (pre.PRECIO * (pre.PIMPUESTO / 100))), 2), 0) AS precioRegular, IFNULL(ROUND((pre.PRECIO + (pre.PRECIO * (pre.PRECIO * 0 + pre.PIMPUESTO / 100))) * (1 - (IFNULL(inv.INVDESCUENTO, 0) / 100)), 2), 0) AS precioDescuento FROM datosb.INV inv LEFT JOIN datosb.preciofinal pre ON inv.ARTICULOID = pre.ARTICULOID AND inv.UNIVENID = pre.UNIDADID LEFT JOIN datosb.UNIDADES uni ON inv.ARTICULOID = uni.ARTICULOID AND inv.UNIBASID = uni.UNIDADID LEFT JOIN datosb.ALM alm ON inv.ARTICULOID = alm.ARTICULOID AND alm.ALMACEN = inv.ALMDEF WHERE (LOWER(TRIM(COALESCE(inv.CLAVE, ''))) LIKE ? OR LOWER(TRIM(COALESCE(inv.DESCRIPCIO, ''))) LIKE ? OR LOWER(TRIM(COALESCE(inv.CLVPROV, ''))) LIKE ?) AND pre.NPRECIO = 1 AND TRIM(COALESCE(inv.CLVPROV, '')) <> '' AND inv.CATALOGO IN ('0', '1', '2') ORDER BY CASE WHEN LOWER(TRIM(COALESCE(inv.DESCRIPCIO, ''))) LIKE ? THEN 1 WHEN LOWER(TRIM(COALESCE(inv.DESCRIPCIO, ''))) LIKE ? THEN 2 WHEN LOWER(TRIM(COALESCE(inv.DESCRIPCIO, ''))) LIKE ? THEN 3 ELSE 6 END ASC, inv.DESCRIPCIO ASC LIMIT ?`;
const [rows] = await conn.execute(sql, ['%mar%','%mar%','%mar%','mar %','% mar %','%mar%',5]);
const mapped = mapCatalogRows(rows);
console.log(JSON.stringify({ ok:true, count: mapped.length, sample: mapped[0] }, null, 2));
await conn.end();
})().catch(err=>{console.error(JSON.stringify({ok:false,message:err.message,stack:err.stack},null,2));process.exit(1);});
